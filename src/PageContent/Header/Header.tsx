import { Button, ThemeWrapper } from "retro-ui";
const Header = () => {
  return (
    <div>
      This is header
      <ThemeWrapper>
        <Button>{"Raw!"}</Button>
      </ThemeWrapper>
    </div>
  );
};

export default Header;
